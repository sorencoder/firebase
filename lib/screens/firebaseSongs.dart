import 'package:flutter/material.dart';

class FirebaseSongs extends StatelessWidget {
  const FirebaseSongs({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
